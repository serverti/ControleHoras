<form method="post" action="{{ path('app_banco_horas_controlador2_delete', {'id': banco_hora.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banco_hora.id) }}">
    <button class="btn">Delete</button>
</form>
